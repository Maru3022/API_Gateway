apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: fitness-platform
  labels:
    app: api-gateway
spec:
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 8075
      targetPort: http
  type: ClusterIP